spring:
  application:
    name: store
  datasource:
    url: jdbc:mysql://localhost:3306/university?createDatabaseIfNotExist=true
    username: root
    password: mysql
  jpa:
    show-sql: true
    open-in-view: false